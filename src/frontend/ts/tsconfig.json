{
  "compilerOptions": {
    "target": "es6",                          // Compila a JavaScript ES6, compatible con navegadores modernos.
    "module": "commonjs",                     // Usa el sistema de módulos estándar de Node.js.
    "outDir": "../static/js",                 // Directorio de salida para los archivos JS compilados.
    "rootDir": "./",                          // Directorio raíz de los archivos de entrada TypeScript.
    "strict": true,                           // Habilita todas las opciones de chequeo de tipos estrictos.
    "esModuleInterop": true,                  // Permite la interoperabilidad con módulos CommonJS.
    "skipLibCheck": true,                     // Omite la verificación de tipos de los archivos de declaración.
    "forceConsistentCasingInFileNames": true  // Asegura que las mayúsculas y minúsculas de los nombres de archivo sean consistentes.
  },
  "include": [
    "**/*.ts"                                 // Incluye todos los archivos .ts en el directorio raíz (./ts) para la compilación.
  ]
}
